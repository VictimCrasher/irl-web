import React from "react";
import "./Hero.scss";
import Spaces from "./Spaces";

const Hero = () => {
	return (
		<div className="hero">
			<Spaces />
			<div>Hero</div>
		</div>
	);
};

export default Hero;
